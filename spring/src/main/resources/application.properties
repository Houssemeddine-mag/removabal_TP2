spring.application.name=spring
server.port=${PORT:8081}

# PostgreSQL Configuration - Render Database
spring.datasource.url=jdbc:postgresql://dpg-d4j32gvpm1nc73dot76g-a:5432/spring_vlc9
spring.datasource.username=spring_vlc9_user
spring.datasource.password=293WYiOJjjKvAKFbTxmnt8GzlYORJTfR
spring.datasource.driver-class-name=org.postgresql.Driver

# Connection Pool Settings (important for Render)
spring.datasource.hikari.maximum-pool-size=5
spring.datasource.hikari.minimum-idle=2
spring.datasource.hikari.connection-timeout=30000
spring.datasource.hikari.idle-timeout=600000
spring.datasource.hikari.max-lifetime=1800000

# JPA/Hibernate Configuration
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=${SHOW_SQL:false}
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.jdbc.lob.non_contextual_creation=true
spring.jpa.open-in-view=false

# Flyway Configuration for Database Migrations
spring.flyway.enabled=true
spring.flyway.locations=classpath:db/migration
spring.flyway.baseline-on-migrate=true
spring.flyway.validate-on-migrate=true

# Logging Configuration
logging.level.root=INFO
logging.level.org.springframework.web=INFO
logging.level.org.hibernate=INFO
logging.level.com.zaxxer.hikari=WARN
logging.pattern.console=%d{yyyy-MM-dd HH:mm:ss} - %msg%n

# Production Settings
spring.profiles.active=${SPRING_PROFILES_ACTIVE:dev}
